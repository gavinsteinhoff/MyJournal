@model IEnumerable<MyJournal.Models.CustomModels.Sharing>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Giver)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Receiver)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.PermissionLevel)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Giver)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Receiver)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PermissionLevel)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SharingKey">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SharingKey">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SharingKey">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
